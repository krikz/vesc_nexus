# VESC Velocity Control Mode - Тестирование

## Описание

Скрипт `test_velocity_control.py` позволяет проверить работу VESC в режиме управления скоростью (Velocity Control Mode) через CAN шину.

В отличие от duty cycle режима, где мы задаём процент напряжения, velocity mode позволяет задавать целевую скорость в RPM, а VESC сам регулирует напряжение для её поддержания.

## Зачем это нужно?

1. **Точность**: VESC с PID регулятором может точнее держать скорость
2. **Стабильность**: При разной нагрузке скорость остаётся постоянной
3. **Меньше осцилляций**: Не нужно вручную подбирать min_duty
4. **Лучший контроль**: Nav2 задаёт скорость → VESC её держит

## Установка зависимостей

```bash
pip install python-can pyyaml
```

## Использование

### 1. Интерактивный режим (ручное управление)

```bash
cd ~/rob_box_project/src/vesc_nexus/src/vesc_nexus/tools
python3 test_velocity_control.py --vesc-id 49 --pole-pairs 15 --interactive
```

**Команды в интерактивном режиме:**
- `r <rpm>` - Задать RPM (например: `r 100` для 100 RPM вперёд, `r -50` для 50 RPM назад)
- `d <duty>` - Задать Duty cycle для сравнения (например: `d 0.1`)
- `s` - Стоп (RPM=0)
- `m [сек]` - Мониторинг статуса (по умолчанию 5 сек)
- `q` - Выход

**Пример сессии:**
```
> r 50        # Разогнать до 50 RPM
📤 Sent: RPM=50 (ERPM=750)
📊 Status: RPM=48.3, Current=0.45A, Duty=0.085

> m 5         # Смотрим как держит скорость 5 секунд
📊 Мониторинг статуса (5.0 сек)...
  Time |      RPM |  Current |   Duty |    Error
------------------------------------------------------------
  0.10 |     49.2 |     0.47 |   0.087 |    ---

> s           # Стоп
⏹️  Остановлено

> q           # Выход
👋 Выход...
```

### 2. Автоматический тест последовательности

Проверяет разные скорости автоматически:

```bash
python3 test_velocity_control.py --vesc-id 49 --pole-pairs 15 --test-sequence
```

Последовательность: 30 → 60 → 100 → 150 → 100 → 50 → 0 → -50 → -100 → -50 → 0 RPM

Каждое значение держится 3 секунды с выводом статистики:
- Средний RPM
- Ошибка регулирования
- Диапазон колебаний

### 3. Тест одного значения RPM

```bash
python3 test_velocity_control.py --vesc-id 49 --pole-pairs 15 --rpm 100
```

Задаёт 100 RPM и мониторит 10 секунд, показывая:
- Фактический RPM
- Ошибку относительно целевого
- Статистику (средний, мин, макс, точность)

## Параметры

### Обязательные

- `--vesc-id` - ID VESC контроллера в CAN сети
  - Left Front: 49 (0x31)
  - Right Front: 124 (0x7C)
  - Left Rear: 81 (0x51)
  - Right Rear: 94 (0x5E)

### Опциональные

- `--can-interface` - CAN интерфейс (по умолчанию: `can0`)
- `--pole-pairs` - Количество пар полюсов мотора (по умолчанию: 15)
  - Для 30-полюсного мотора: `--pole-pairs 15`

### Режим работы (один из)

- `--interactive` - Ручное управление с клавиатуры
- `--test-sequence` - Автоматический тест
- `--rpm <значение>` - Тест конкретной скорости

## Примеры использования

### Проверить все колёса по очереди

```bash
# Left Front
python3 test_velocity_control.py --vesc-id 49 --interactive

# Right Front  
python3 test_velocity_control.py --vesc-id 124 --interactive

# Left Rear
python3 test_velocity_control.py --vesc-id 81 --interactive

# Right Rear
python3 test_velocity_control.py --vesc-id 94 --interactive
```

### Проверить точность при 100 RPM

```bash
python3 test_velocity_control.py --vesc-id 49 --rpm 100
```

Ожидаемый вывод:
```
📈 Статистика:
   Target RPM:  100.0
   Actual RPM:  98.7 (avg)
   Error:       -1.3 (-1.3%)
   Range:       96.2 ... 101.5
```

### Найти минимальный стабильный RPM

```bash
python3 test_velocity_control.py --vesc-id 49 --interactive

# В интерактивном режиме попробуйте:
> r 10
> m 5
> r 20
> m 5
> r 30
> m 5
```

Смотрите на статус - когда колесо начинает стабильно вращаться без рывков.

## Интерпретация результатов

### Хорошие признаки ✅

- **Ошибка < 5%** - VESC точно держит скорость
- **Малый диапазон** (Range < 10% от target) - стабильное регулирование
- **Duty плавно меняется** - PID регулятор работает корректно
- **Ток стабильный** - нет резких рывков

### Проблемные признаки ⚠️

- **Ошибка > 10%** - VESC не может достичь целевой скорости
- **Большой диапазон** - осцилляции, нестабильность
- **Duty быстро меняется** - PID требует настройки
- **Ток скачет** - механические проблемы или перегрузка

## Сравнение с Duty Cycle режимом

| Параметр | Duty Cycle | Velocity Control |
|----------|-----------|------------------|
| Команда | % напряжения (0-100%) | RPM |
| Стабильность | Зависит от нагрузки | Постоянная |
| Точность | Низкая | Высокая |
| Регулирование | Вручную в Nav2 | Автоматически в VESC |
| Min speed | Нужен min_duty/min_speed_xy | Достаточно min_rpm |

## Применение для Rob Box

После успешного теста можно:

1. **Настроить vesc_driver** для использования velocity mode
2. **Изменить vesc_config.yaml**:
   ```yaml
   control_mode: "velocity"  # Вместо "duty"
   min_rpm: 30.0             # Вместо min_duty
   ```

3. **Упростить nav2_params.yaml**:
   - Убрать min_speed_xy (VESC сам справится)
   - Убрать velocity_smoother deadband
   - PID регулирование на стороне VESC

4. **Профит**:
   - Робот стабильнее движется
   - Нет осцилляций при старте
   - Лучше реакция на препятствия

## Безопасность

⚠️ **ВАЖНО:**
- Поднимите робота на подставку (колёса не должны касаться пола)
- Начинайте с малых RPM (10-30)
- Держите E-STOP под рукой
- При странном поведении нажмите Ctrl+C

## Отладка

### CAN шина не работает

```bash
# Проверить интерфейс
ip link show can0

# Поднять интерфейс
sudo ip link set can0 up type can bitrate 500000

# Проверить трафик
candump can0
```

### VESC не отвечает

```bash
# Проверить что VESC отправляет статус
candump can0 | grep "09"  # CAN_PACKET_STATUS

# Проверить CAN ID VESC
candump can0 -L  # Покажет все ID в сети
```

### Неверный RPM

- Проверьте `--pole-pairs` (для 30 полюсов = 15 пар)
- Формула: `mechanical_rpm = erpm / pole_pairs`

## TODO / Идеи для улучшения

- [ ] График RPM в реальном времени (matplotlib)
- [ ] Логирование в CSV для анализа
- [ ] PID tuning mode (автоподбор коэффициентов)
- [ ] Сравнение duty vs velocity на одних и тех же скоростях
- [ ] Тест с постепенным увеличением нагрузки

## Связанные файлы

- `calibrate_max_rpm.py` - Калибровка максимальных оборотов
- `vesc_config.yaml` - Конфигурация VESC драйвера
- `nav2_params.yaml` - Настройки навигации

---

**Автор:** Rob Box Team  
**Дата:** 2025-12-16  
**Версия:** 1.0
